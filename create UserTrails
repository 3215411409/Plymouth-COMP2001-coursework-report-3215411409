CREATE TABLE UserTrails (
    UserTrailID INT PRIMARY KEY IDENTITY(1,1),
    UserID INT NOT NULL,
    TrailID INT NOT NULL,
    FOREIGN KEY (UserID) REFERENCES Users(UserID),
    FOREIGN KEY (TrailID) REFERENCES Trails(TrailID)
);
